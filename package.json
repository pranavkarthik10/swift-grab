{
  "name": "swift-grab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["web", "bridge"],
  "scripts": {
    "dev:web": "bun --filter swift-grab-web dev",
    "dev:bridge": "bun --filter swift-grab-bridge dev",
    "build:web": "bun --filter swift-grab-web build",
    "typecheck": "bun --filter '*' typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
